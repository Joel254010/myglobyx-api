# ---------------------------------------
# Server
# ---------------------------------------
# Em dev você pode usar 5000. No Render a PORT é injetada automaticamente.
PORT=5000
NODE_ENV=development
# Prefixo base das rotas (o app monta /api/...)
API_PREFIX=/api

# ---------------------------------------
# Auth / JWT
# ---------------------------------------
# Troque em produção por um segredo forte (>= 32 chars)
JWT_SECRET=troque-por-um-segredo-forte
# 7d, 12h, 3600s etc.
TOKEN_EXPIRES_IN=7d
# (opcionais) se quiser travar emissor/destinatário
JWT_ISSUER=
JWT_AUDIENCE=

# ---------------------------------------
# CORS
# ---------------------------------------
# Aceita 1 ou várias origens separadas por vírgula (sem barra no final)
# Ex.: https://my-globyx.netlify.app,https://myglobyx.com,http://localhost:5173
CORS_ORIGIN=https://my-globyx.netlify.app,http://localhost:5173

# (opcional/legado) se alguma parte do front ler isto
FRONTEND_URL=http://localhost:5173

# ---------------------------------------
# Database (MongoDB Atlas)
# ---------------------------------------
# Use a URI com o db no final (ex.: /myglobyx)
MONGODB_URI=

# ---------------------------------------
# Admin seed
# ---------------------------------------
# Lista de e-mails admin (separados por vírgula)
ADMIN_EMAILS=admin@myglobyx.com
# Senha inicial (altere depois)
ADMIN_SEED_PASSWORD=123456
# Habilita o seed automático ao iniciar
AUTO_SEED_ADMIN=true

# ---------------------------------------
# Crypto
# ---------------------------------------
# bcryptjs rounds (8–12). 10 é um bom equilíbrio no Render.
BCRYPT_ROUNDS=10

# ---------------------------------------
# Integrações futuras
# ---------------------------------------
# Segredo do webhook da Appmax (placeholder até ativarmos)
APPMAX_WEBHOOK_SECRET=
